export default class OctaveLayout {
  TL: number;
  TR: number;
  BL: number;
  BR: number;

  constructor(TL: number, TR: number, BL: number, BR: number) {
    this.TL = TL;
    this.TR = TR;
    this.BL = BL;
    this.BR = BR;
  }
}